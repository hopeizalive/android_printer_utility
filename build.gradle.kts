// Root (project-level) Gradle file: plugin versions shared by all modules.
// The actual app binary, SDK levels, and dependencies live in app/build.gradle.kts.
plugins {
    id("com.android.application") version "8.7.2" apply false
}
